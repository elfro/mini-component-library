import { Meta, Story } from '@storybook/addon-docs/blocks';
import CustomProgressBar from './CustomProgressBar';

<Meta
  title="ProgressBar"
  component={CustomProgressBar}
  argTypes={{
    value: {
      control: {
        type: 'number',
        min: 0,
        max: 100,
      },
    },
    size: {
      control: {
        type: 'select',
        options: ['small', 'medium', 'large'],
      },
    },
  }}
/>


export const Template = (args) => <CustomProgressBar {...args} />;

# ProgressBar

<Story name="Custom" args={{ value: 50, size: 'medium' }}>
    {Template.bind({})}
</Story>